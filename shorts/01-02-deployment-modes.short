# Where does Spark actually run?

- **local** — driver + executors in one JVM on your laptop.
- **YARN** — on Hadoop clusters.
- **Kubernetes** — driver and executors as pods.
- **Databricks** — managed; the manager is hidden.
- **Same code — only where it runs changes.**
